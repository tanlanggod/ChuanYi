# 接口完成度清单（2026-02-27）

## 1. App 端接口完成度

已完成（已在 App 页面联调）：

1. 认证：`/api/auth/login`、`/api/auth/me`、`/api/auth/logout`
2. DIY：`/api/sku/categories`、`/api/sku/list`、`/api/design/draft/*`、`/api/design/snapshot/*`
3. 地址：`/api/address/list`、`/api/address/save`、`/api/address/{id}`
4. 订单：`/api/order/create`、`/api/order/create-from-cart`、`/api/order/list`、`/api/order/{orderNo}`、`/api/order/{orderNo}/confirm-receipt`
5. 支付：`/api/payment/mock/pay`、`/api/payment/mock/callback`
6. 内容：`/api/content/banner/list`、`/api/gallery/list`
7. 物流：`/api/logistics/track`
8. 好物：`/api/goods/categories`、`/api/goods/list`、`/api/goods/{spuId}`
9. 购物车：`/api/cart`、`/api/cart/items`、`/api/cart/items/{cartItemId}`、`/api/cart/items/{cartItemId}/select`、`/api/cart/items/select-all`、`/api/cart/settlement/preview`

待加强（功能可用但后续建议）：

1. 物流当前为模拟轨迹，尚未接入真实快递平台。
2. 支付为 mock 流程，尚未接入真实支付网关。

## 2. Admin 端接口完成度

已完成（已在管理端页面联调）：

1. 管理员认证：`/api/admin/auth/login`、`/api/admin/auth/me`、`/api/admin/auth/logout`
2. 看板：`/api/admin/dashboard/overview`
3. 订单：`/api/admin/order/list`、`/api/admin/order/{orderNo}`、`/api/admin/order/{orderNo}/cancel`、`/api/admin/order/{orderNo}/ship`、`/api/admin/order/{orderNo}/complete`
4. 好物 SPU：`/api/admin/goods/list`、`/api/admin/goods/{spuId}`、`/api/admin/goods/{spuId}/status`、`/api/admin/goods/{spuId}/save`
5. 好物 SKU：`/api/admin/goods/{spuId}/sku/create`、`/api/admin/goods/sku/{skuId}/save`、`/api/admin/goods/sku/{skuId}/status`、`/api/admin/goods/sku/{skuId}`
6. 内容 Banner：`/api/admin/content/banners`、`/api/admin/content/banners/{id}/status`、`/api/admin/content/banners/save`
7. 内容广场：`/api/admin/content/gallery`、`/api/admin/content/gallery/{id}/status`、`/api/admin/content/gallery/{id}/save`
8. 内容图片上传：`/api/admin/content/upload`（返回可访问 URL）

待加强（后续建议）：

1. 管理员角色权限（RBAC）尚未拆分，当前为单管理员能力。
2. Banner/广场暂不支持图片上传，仅支持 URL 字段编辑。

## 3. 数据库与初始化

已完成：

1. MySQL 结构与初始化 SQL：`backend/src/main/resources/db/schema_v1.sql`、`backend/src/main/resources/db/data_v1.sql`
2. Spring Boot 启动自动初始化：`spring.sql.init.*`

待加强：

1. 生产环境建议关闭自动初始化并使用 Flyway/Liquibase 做版本迁移。
